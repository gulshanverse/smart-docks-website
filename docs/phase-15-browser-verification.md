
## Chromium persistence scenario

Created a local project in the Projects workspace. The project dashboard appeared with `ACTIVE` status, zero documents, zero workflows, and zero stored bytes. After uploading the synthetic `phase12-fixture.png`, the UI continued to label it as a temporary document and exposed an explicit `Save to project` action. After activation, the project updated to one document and 101 B stored, the document library showed one validated immutable original version, and project history showed `PROJECT CREATED` and `DOCUMENT SAVED`. Existing Phase 12–14 workflow surfaces remained visible below the project workspace.
